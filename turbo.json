{
  "baseBranch": "origin/main",
  "pipeline": {
    "build": {
      "dependsOn": ["generate", "^build"],
      "outputs": ["dist/**"]
    },
    "generate": {},
    "dev": {
      "dependsOn": ["generate"],
      "cache": false
    },
    "start": {}
  }
}
