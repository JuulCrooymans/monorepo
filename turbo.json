{
  "baseBranch": "origin/main",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "generate": {},
    "dev": {
      "dependsOn": ["generate"],
      "cache": false
    }
  }
}
