overwrite: true
schema: "graphql/**/*.graphql"
generates:
  types/graphql.generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      contextType: ./graphql#Context
